# 화면 캡처

`REPORT.md` §5 데모 설계에서 참조하는 이미지를 둔다.

| 파일명 | 내용 | 만드는 법 |
|---|---|---|
| `screenshot_answer.png` | 정상 답변 화면 | `streamlit run app.py` → 예시 버튼 **H2-01** |
| `screenshot_refusal.png` | 거절 화면 | 예시 버튼 **R-02** |

정상 답변 캡처는 **출처 expander를 펼친 상태**로 찍으면 근거 추적이 드러난다.
거절 화면은 "근거가 없을 때 지어내지 않는다"는 이 프로젝트의 핵심을 보여주는 장면이다.
