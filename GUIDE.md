# 디자인사 GraphRAG 에이전트 — 구현 안내서

수업에서 만든 한국 영화 GraphRAG를 **디자인사(Design History)** 도메인으로 옮긴다.
이 문서는 워커들이 따르는 단일 기준이다. 결정과 그 근거를 여기 적고, `REPORT.md`는 여기서 파생시킨다.

---

## 1. 과제 요구사항 체크리스트

| # | 요구사항 | 담당 | 상태 |
|---|---|---|---|
| 1 | 개체가 겹치는 코퍼스 50건 이상 | W1 | ☐ |
| 1 | 주제·소스 채택/탈락 기준 기록 | W1 | ☐ |
| 1 | 평가셋 10건 이상 (기대 정답·기대 경로·원문 근거) | **Claude** | ☐ |
| 2 | 스키마로 제한한 추출 | W2 | ☐ |
| 2 | 추출 후 정규화 (표기 통일·별칭 병합·일반명사 제외) | W2 | ☐ |
| 3 | 시작 개체 → n홉 확장 → 실제 경로 기록 | W3 | ☐ |
| 3 | 홉 상한·허브 배제 기준과 그 이유 | **Claude** | ☑ (§5) |
| 3 | 근거만으로 답변, 근거 없으면 거절 | W3 | ☐ |
| 4 | 기대 경로 재현율 측정 | W4 | ☐ |
| 4 | 실패를 색인·탐색·생성으로 분류 | W4 | ☐ |
| 4 | 홉 수별 basic RAG 대조 | W4 | ☐ |
| 5 | 웹 데모 (답변 + 경로 + 삼중항 + 출처) | W5 | ☐ |
| 5 | README 실행법 + 화면 캡처 | W5 | ☐ |
| 제출 | `REPORT.md` 6개 섹션 + 깃헙 URL | **Claude** | ☐ |

> 제출은 파일/URL 중 **하나만**, **제출 후 수정 불가**. 최종 점검 전에는 제출하지 않는다.

---

## 2. 도메인과 스키마 (확정)

### 왜 디자인사인가
인물·학교·사조·작품·제조사가 **서로를 반드시 경유해야만** 답이 나오는 구조다.
"바우하우스 교수 → 미국 망명 → 새 학교 설립 → 그 학교 출신 디자이너 → 그가 만든 의자 → 그 의자를 생산한 회사"처럼
한 문서만 읽어서는 절대 답할 수 없는 질문이 자연스럽게 나온다. 멀티홉 증명에 유리하다.

### 노드 (5종)

| 타입 | 설명 | 예시 |
|---|---|---|
| `Person` | 디자이너·건축가·교육자 | 발터 그로피우스, 디터 람스, 찰스 임스 |
| `Movement` | 사조·운동 | 바우하우스(사조), 데 스테일, 미술공예운동 |
| `Institution` | 학교·기관 | 바우하우스(학교), 울름 조형대학, 뉴 바우하우스 |
| `Work` | 작품·제품·서체 | 바실리 체어, 헬베티카, 브라운 SK4 |
| `Company` | 제조사·스튜디오 | 놀, 허먼 밀러, 비트라, 브라운 |

### 관계 (7종)

| 관계 | 방향 | 의미 |
|---|---|---|
| `STUDIED_AT` | Person → Institution | 수학 |
| `TAUGHT_AT` | Person → Institution | 교수·강의 |
| `FOUNDED` | Person → Institution \| Company | 설립 |
| `BELONGS_TO` | Person \| Work → Movement | 사조 소속 |
| `DESIGNED` | Person → Work | 디자인 |
| `MANUFACTURED_BY` | Work → Company | 생산 |
| `INFLUENCED_BY` | Person \| Movement → Person \| Movement | 영향 |

### 뽑지 않기로 한 관계와 그 대가

`REPORT.md`에 반드시 들어갈 항목이다. 의도적으로 버린 것들:

| 버린 관계 | 이유 | 대가 |
|---|---|---|
| `BORN_IN` / `DIED_IN` (지역) | 국가·도시 노드는 차수가 폭발해 모든 인물을 연결하는 가짜 다리가 된다 | "독일 출신 디자이너" 류 질문에 답 못 함 |
| `WORKED_WITH` (협업) | 위키 본문에서 단순 동시 언급과 구분이 안 돼 정밀도가 무너진다 | 협업 관계 질문 불가 |
| 연도·시기 | 노드로 만들면 허브가 되고, 속성으로 두면 멀티홉에 기여하지 않는다 | 시대 비교 질문 불가 |
| `PART_OF` (사조 계층) | 사조 간 포함 관계는 문헌마다 달라 정답을 정할 수 없다 | 사조 계층 탐색 불가 |

**원칙: 차수가 폭발하는 관계는 멀티홉의 적이다.** 경로가 짧아 보여도 의미 없는 경로가 된다.

---

## 3. 코퍼스 수집 정책

**한국어 + 영문 위키백과 병용.** 디자인사는 한국어 문서가 얇아 800자 필터에 대량 탈락한다.
한국어를 우선하되 부족분을 영문으로 채운다.

### 시드 (한국어 우선, 영문 보충)

```
한국어: 바우하우스, 발터 그로피우스, 루트비히 미스 반 데어 로에, 르 코르뷔지에,
        마르셀 브로이어, 데 스테일, 미술공예운동, 디터 람스, 울름 조형대학, 찰스 임스
영문:   Bauhaus, Walter Gropius, Ludwig Mies van der Rohe, Marcel Breuer,
        László Moholy-Nagy, Josef Albers, De Stijl, Dieter Rams, Ulm School of Design,
        Herman Miller, Knoll, Vitra, Charles and Ray Eames, Massimo Vignelli, Paul Rand
```

### 채택 / 탈락 기준 (로그로 남길 것)

**채택**: 시드와 분류(category)를 1개 이상 공유 · 본문 800자 이상 · 네임스페이스 0
**탈락**: 연도/목록/틀/분류 문서 · 토막글 · 스키마 5개 노드 타입 중 어디에도 안 맞는 문서
**목표**: 60~80건 (한국어 최소 25건). `data/manifest.json`에 언어·시드·채택·탈락 사유를 건별로 남긴다.

---

## 4. 정규화 규칙

한/영 병용이라 **별칭 병합이 이 프로젝트의 핵심 난제**다. 여기를 잘 처리한 기록이 곧 점수다.

1. **한영 동일 개체 병합** — `바우하우스` = `Bauhaus`, `미스 반 데어 로에` = `Ludwig Mies van der Rohe`
   → 위키데이터 QID를 정준 키로 쓴다. 없으면 수기 별칭 사전(`data/aliases.json`)으로 처리.
2. **표기 흔들림 통일** — `미스 반 데어 로에` / `미스 반 데 로에` / `Mies van der Rohe`
3. **동명이의 분리** — `바우하우스(학교, Institution)` ≠ `바우하우스(사조, Movement)`. **이건 병합하면 안 된다.**
   타입이 다르면 별개 노드로 유지한다.
4. **일반명사 제외** — 디자인, 건축, 모더니즘(일반 용법), 예술, 가구 등은 노드로 만들지 않는다.
5. **병합 로그** — 무엇을 무엇에 합쳤는지 `output/merge_log.json`에 남긴다. 실패 분석 때 색인 오류를 여기서 추적한다.

---

## 5. 멀티홉 정책 (확정)

### 홉 상한: **3홉**
골든셋이 2~3홉 중심이고, 4홉 이상은 근거 체인이 길어져 (a) 생성 단계 환각 위험이 급증하고
(b) 경로 재현율이 사실상 측정 불가능해진다. 3홉에서 끊고, 답이 없으면 **넓히지 말고 모른다고 한다.**

### 허브 배제 기준
1. **차수 기반** — 그래프 전체에서 연결 차수 상위 5% 노드는 **경유 금지**(시작·도착 노드로는 허용).
2. **타입 기반** — `Movement` 노드는 경유를 1회로 제한. 사조는 수십 명을 무차별 연결해 가짜 경로를 만든다.
3. 배제로 인해 답을 못 찾은 건은 실패로 처리하지 않고 **"허브 차단으로 중단"**으로 별도 집계한다.

### 거절 조건
근거 삼중항이 0개거나, 찾은 경로가 질문의 관계 타입과 맞지 않으면 답을 생성하지 않는다.
**"근거를 찾지 못했습니다"를 출력하고 어디까지 탐색했는지 보여준다.**

---

## 6. 골든셋 설계 (12건)

| 홉 수 | 건수 | 목적 |
|---|---|---|
| 1홉 | 2 | 기본 색인 동작 확인 (basic RAG도 맞혀야 정상) |
| 2홉 | 5 | 멀티홉 핵심 구간 |
| 3홉 | 3 | 상한 경계 동작 |
| **거절** | **2** | **근거 없는 질문에 지어내지 않는지** |

거절 케이스 2건은 루브릭의 *"근거가 없을 때 지어내지 않고 거절하는가"*에 직결된다. 반드시 넣는다.

각 문항 필드: `question` · `expected_answer` · `expected_path`(관계 타입의 순서·방향) · `evidence`(원문 문서명+문장) · `hops`

**예시 (2홉)**
```
Q: 바우하우스에서 가르쳤던 인물이 미국에 세운 학교는?
expected_path: Person -TAUGHT_AT-> Institution(바우하우스)  /  Person -FOUNDED-> Institution
expected_answer: 뉴 바우하우스 (라슬로 모호이너지)
```

**예시 (3홉)**
```
Q: 바실리 체어를 디자인한 사람과 같은 사조에 속한 다른 디자이너의 작품은?
expected_path: Work -DESIGNED<- Person -BELONGS_TO-> Movement <-BELONGS_TO- Person -DESIGNED-> Work
```

---

## 7. 평가 설계

1. **경로 재현율** — 기대 경로의 관계 삼중항이 실제 탐색 근거에 몇 개나 들어왔는가 (부분 점수 허용).
2. **홉 수별 분리 보고** — 평균 내지 않는다. 1홉/2홉/3홉/거절을 각각 보고한다.
3. **basic RAG 대조** — 같은 질문을 벡터 검색 top-k로만 답하게 하고 홉 수별로 비교한다.
   2~3홉에서 벌어지는 격차가 이 프로젝트의 존재 이유다.
4. **실패 3층 분류** — 틀린 건은 반드시 하나로 귀속시킨다.

| 층 | 판정 기준 |
|---|---|
| **색인** | 필요한 삼중항이 그래프에 아예 없음 (추출 실패 / 병합 실패) |
| **탐색** | 삼중항은 있는데 경로를 못 찾음 (시작 개체 오인 / 허브 차단 / 홉 상한) |
| **생성** | 근거는 맞는데 답변이 틀림 (환각 / 근거 무시) |

---

## 8. 오케스트레이션

### 모델 분담

| 모델 | 담당 | 이유 |
|---|---|---|
| **Claude (나)** | 스키마·홉 정책·골든셋 설계, 워커 산출물 검수, `REPORT.md` | 루브릭 3축이 전부 여기 걸려 있다 |
| **Muse Spark 1.3** (무료) | 코드 구현 전량, 대량 추출 실행 | 토큰 무료. 1M 컨텍스트 |

호출:
```bash
opencode run -m "opencode/muse-spark-1.3-contributor-free" "<지시>"
```
런타임(파이썬 안)에서는 OpenAI 호환 엔드포인트 시도:
```python
client = OpenAI(base_url="https://opencode.ai/zen/v1", api_key=os.environ["OPENCODE_API_KEY"])
```

> ⚠️ Meta가 프롬프트/응답을 학습에 쓸 수 있다. 공개 위키 데이터만 다루므로 문제없으나 개인정보는 넣지 않는다.
> ⚠️ 무료 쿼터가 비공개·유동적이다. 워커 작업은 잘게 쪼개고 산출물을 즉시 파일로 남겨 재개 가능하게 한다.

### 워커 (5개)

| 워커 | 담당 | 산출물 | 선행 |
|---|---|---|---|
| **W1 코퍼스** | 한/영 위키 수집, 채택·탈락 로그 | `data/docs/`, `manifest.json` | — |
| **W2 그래프** | `build_graph.py` — 스키마 제한 추출 + 정규화 | `graph.graphml`, `merge_log.json` | W1 |
| **W3 에이전트** | `agent.py` — 시작 개체→3홉 확장→경로 기록→근거 답변/거절 | `runs.jsonl` | W2 |
| **W4 평가** | `evaluate.py` — 홉수별 채점 + basic RAG 대조 + 실패 분류 | `eval.json` | W3 |
| **W5 데모** | `app.py` — streamlit, 답변+경로+삼중항+출처 | 캡처, README | W3 |

**실행 순서**: `W1 ‖ W5(껍데기)` → `W2` → `W3` → `W4 ‖ W5(완성)`

각 워커는 끝나면 산출물 경로와 **막힌 지점**을 보고한다. Claude가 검수 후 다음 워커를 띄운다.

---

## 9. 리스크와 폴백

| 리스크 | 징후 | 폴백 |
|---|---|---|
| Zen OpenAI 호환 엔드포인트가 이 모델을 거부 | `/chat/completions` 400/404 | `/responses` 엔드포인트 → 그래도 안 되면 Ollama 로컬 소형 모델 |
| 무료 쿼터 소진 | 워커 중간 실패 | 산출물 파일 기준으로 재개. 남은 분량만 재실행 |
| 한국어 코퍼스 50건 미달 | W1 수집 결과 부족 | 영문 비중 상향 + 시드 확대 |
| 한영 병합 실패로 그래프 분단 | 2홉 재현율 급락 | `aliases.json` 수기 보강 (병합 로그로 추적) |
| 개체 겹침 부족 | 1홉으로 다 풀림 | 시드를 인물 중심으로 재구성해 공통 학교·회사를 늘림 |

---

## 10. 다음 액션

1. W1 디스패치 — 한/영 위키 수집 스크립트 작성 및 실행
2. W1 결과 검수 후 Claude가 골든셋 12건 작성 (실제 코퍼스 근거 기반)
3. W2 디스패치

> 골든셋은 **코퍼스를 확보한 뒤** 만든다. 실제 문서에 근거가 없는 문항을 만들면 평가 전체가 무의미해진다.
